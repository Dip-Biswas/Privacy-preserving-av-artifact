# CrUX domain lists

Place country exports here as `{code}-top100k.csv` with columns `origin,rank`.

They are gitignored (large, and CrUX redistribution may be restricted). A tiny smoke-test file is committed:

- [`example-domains.csv`](example-domains.csv)

How to build a real list: [`docs/CRAWL.md`](../../docs/CRAWL.md) (domain-list section).

Codes we used: `gb`, `au`, `tx` (US list, Texas vantage), `fr`, `id`, `ca`.
